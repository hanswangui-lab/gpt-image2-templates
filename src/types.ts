export type TemplateItem = {
  id: string
  title: string
  category: string
  categoryLabel: string
  description: string
  tags: string[]
  prompt: string
  gradient: string
  accent: string
  previewTitle: string
  image?: string
  sourceLabel?: string
  sourceUrl?: string
  githubUrl?: string
}
